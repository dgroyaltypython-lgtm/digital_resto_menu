function getCookie(n) { const m = document.cookie.match(new RegExp('(^|; )' + n + '=([^;]*)')); return m ? decodeURIComponent(m[2]) : '' }
async function request(type) { const m = document.getElementById('message'); m.textContent = 'Sending request…'; try { const r = await fetch('/api/service-call/', { method: 'POST', credentials: 'same-origin', headers: { 'Content-Type': 'application/json', 'X-CSRFToken': getCookie('csrftoken') }, body: JSON.stringify({ table: window.TABLE_NO, request_type: type }) }); const d = await r.json(); m.textContent = d.message || 'Request sent.' } catch (e) { m.textContent = 'Connection problem. Please try again.' } }
document.querySelectorAll('.service-buttons button').forEach(b => b.onclick = () => request(b.dataset.type));
const s = document.getElementById('search'), chips = document.querySelectorAll('.chip'), cards = document.querySelectorAll('.dish-wrap'), sections = document.querySelectorAll('.menu-section'), empty = document.getElementById('empty');
function filter() { const q = s.value.toLowerCase().trim(), active = document.querySelector('.chip.active')?.dataset.cat; let n = 0; cards.forEach(c => { const ok = (!q || c.dataset.name.includes(q)) && (!active || active === 'all' || c.dataset.cat === active); c.classList.toggle('d-none', !ok); if (ok) n++ }); sections.forEach(sec => sec.classList.toggle('d-none', ![...sec.querySelectorAll('.dish-wrap')].some(x => !x.classList.contains('d-none')))); empty.classList.toggle('d-none', n !== 0) }
chips.forEach(c => c.onclick = () => { chips.forEach(x => x.classList.remove('active')); c.classList.add('active'); filter() }); s.oninput = filter;
